import GenerateIdeas from '~/interfaces/ai/generateIdeas'

const generateIdeas: GenerateIdeas = async (skill) => {
  // いなちゃんのタスク
  // ChatGPT呼び出す
  skill.database.join(','); // 配列から文字列に変換
  return []
}

export default generateIdeas
